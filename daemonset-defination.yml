apiVersion: apps/v1

kind: DaemonSet

metadata:
  name: monitoring-daemonset
  labels:
    app: monitoring

spec:
  selector:
    matchLeables:
      app: monitoring
  template:
    metadata:
      lables:
        app: monitoring
    containers:
      - name: elkstack
        image: elasticsearch
